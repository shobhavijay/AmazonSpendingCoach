# ── LangGraph ──────────────────────────────────────────────────
langgraph>=0.2.0

# ── LLM ────────────────────────────────────────────────────────
langchain-anthropic>=0.3.15
langchain-core>=0.3.0
anthropic>=0.52.0

# ── Environment ─────────────────────────────────────────────────
python-dotenv>=1.1.0

# ── Spending forecast ───────────────────────────────────────────
# Uncomment the block for your chosen FORECAST_MODEL, then pip install.
#
# FORECAST_MODEL=prophet  (default — better for seasonal/gapped data)
# prophet>=1.1.5
# pandas>=2.0.0
#
# FORECAST_MODEL=arima  (lightweight — good for continuous monthly series)
# statsmodels>=0.14.0
# pandas>=2.0.0
#
# pandas is required by both models — install once regardless of model choice.

# ── Dev / testing ───────────────────────────────────────────────
pytest>=8.3.5
pytest-asyncio>=0.23.0

# ── Removed vs files/ ──────────────────────────────────────────
# langchain           — replaced by LangGraph orchestration
# langchain-openai    — not used
# langchain-community — removed (was only for CSVLoader)
# openai              — not used
# unstructured        — removed (was CSVLoader dependency)
